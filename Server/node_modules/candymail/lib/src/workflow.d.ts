import { Workflow, BodyParam } from './types';
declare const setWorkflows: (workflows: Workflow[]) => void;
declare const getWorkflows: () => Workflow[];
declare const runWorkflow: (workflow: string, sendTo: string, params?: BodyParam[]) => Promise<void>;
export { setWorkflows, runWorkflow, getWorkflows };
